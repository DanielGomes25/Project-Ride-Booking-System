{
  // Root config for project references used by `tsc -b`.
  // Keep compiler options in the referenced configs below.
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
